{{! si existe un error, haz esto... }}
{{#if errors}}
  <div class="alert alert-warning alert-dismissible fade show" role="alert">
    {{text}}
    <strong>Error!</strong>
    you must complete all the fields to create the note.
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="alert"
      aria-label="Close"
    ></button>
  </div>
{{/if}}